// Runtime configuration — this file is rewritten by entrypoint.sh at container
// startup so that RUNTIME_API_URL reflects the actual backend domain without
// requiring a rebuild.
window.RUNTIME_API_URL = '';
